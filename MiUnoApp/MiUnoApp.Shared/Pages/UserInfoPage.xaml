<Page
    x:Class="MiUnoApp.Pages.UserInfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MiUnoApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <TextBlock Text="Account" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBox x:Name="emailTxt" PlaceholderText="Email" Margin="0,10,0,0"/>
            <TextBox x:Name="userNameTxt" PlaceholderText="User name" Margin="0,5,0,0"/>
            <TextBox x:Name="firstNamesTxt" PlaceholderText="First names" Margin="0,5,0,0"/>
            <TextBox x:Name="lastNamesTxt" PlaceholderText="Last names" Margin="0,5,0,0"/>
            <TextBox x:Name="phoneTxt" PlaceholderText="Phone number" Margin="0,5,0,0"/>
            <Button HorizontalAlignment="Center" Content="Update" Height="30" Width="100" Margin="0,10,0,0" Click="{x:Bind OnUpdateBtnClick}"/>
        </StackPanel>
    </Grid>
</Page>
